const navToggle=document.querySelector('[data-nav-toggle]');const nav=document.querySelector('[data-nav]');if(navToggle&&nav){const label=navToggle.querySelector('.sr-only');const visible=navToggle.querySelector('span[aria-hidden="true"]');const set=open=>{nav.classList.toggle('open',open);navToggle.setAttribute('aria-expanded',String(open));if(label)label.textContent=open?'Cerrar navegación':'Abrir navegación';if(visible)visible.textContent=open?'Cerrar':'Menú'};navToggle.addEventListener('click',()=>set(!nav.classList.contains('open')));nav.addEventListener('click',e=>{if(e.target.closest('a'))set(false)});document.addEventListener('click',e=>{if(nav.classList.contains('open')&&!e.target.closest('[data-nav], [data-nav-toggle]'))set(false)});document.addEventListener('keydown',e=>{if(e.key==='Escape')set(false)})}const year=document.querySelector('[data-year]');if(year)year.textContent=new Date().getFullYear();document.querySelectorAll('[data-filter]').forEach(btn=>{btn.addEventListener('click',()=>{const group=btn.closest('.events-calendar');if(!group)return;const filter=btn.dataset.filter;group.querySelectorAll('[data-filter]').forEach(b=>b.classList.toggle('active',b===btn));group.querySelectorAll('[data-category]').forEach(card=>{card.hidden=!(filter==='all'||card.dataset.category===filter)})})});